<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.caresomebody.test.consumerapp">

    <uses-permission android:name="com.caresomebody.test.submisi2fundamental.READ_DATABASE" android:protectionLevel="normal"/>
    <uses-permission android:name="com.caresomebody.test.submisi2fundamental.WRITE_DATABASE" android:protectionLevel="normal"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submisi2Fundamental">
        <activity android:name=".FavoriteUserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <queries>
        <package android:name="com.caresomebody.test.submisi2fundamental" />
    </queries>

</manifest>